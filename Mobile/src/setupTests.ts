import mockAsyncStorage from '@react-native-community/async-storage/jest/async-storage-mock';
// n precisa tipar isso, professor deixou assim;

jest.mock('@react-native-community/async-storage', () => mockAsyncStorage);
